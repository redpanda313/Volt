# Web export output

CI writes the Godot HTML5 build here (`index.html`, `.js`, `.wasm`, `.pck`).

Do not commit the binaries. Use `tools/export_web.sh` locally or the **Deploy HTML5 to GitHub Pages** workflow.
